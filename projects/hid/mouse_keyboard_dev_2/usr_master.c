#include "usr_master.h"

void usr_master_init(void)
{
    // // Initialize the UART
    // usr_uart_init();
    // // Initialize the USB device
    // usr_usbd_init();
    // // Initialize the slave
    // usr_slave_init();
}